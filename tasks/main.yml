---
# Web role tasks
- include_vars: "{{ ansible_os_family }}.yml"
  tags:
    - rubedo
    - always

- include: install.yml
  tags:
    - rubedo
    - install

- include: configure.yml
  tags:
    - rubedo
    - configure

- include: delivery-user.yml
  when: rubedo_add_delivery_user
  tags:
    - rubedo
    - delivery-user
