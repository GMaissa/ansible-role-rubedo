---
rubedo_vhosts:
  rubedo:
    enabled: yes
    listen: '*:80'
    root: /var/www/rubedo
    name: rubedo.local
    aliases:
      - www.rubedo.local

rubedo_php_timezone: Europe/Paris

rubedo_public_keys:
  - ~/.ssh/id_rsa.pub

rubedo_add_delivery_user: false
rubedo_delivery_account: rubedo
