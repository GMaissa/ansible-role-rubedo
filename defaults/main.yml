---
rubedo_web:
  vhosts:
    rubedo:
      enabled: yes
      listen: '*:80'
      root: /var/www/rubedo
      name: rubedo.local
      aliases:
        - www.rubedo.local
  
  php_timezone: Europe/Paris
  
  public_key_files:
      - ~/.ssh/id_rsa.pub
  
  delivery_account: delivery
